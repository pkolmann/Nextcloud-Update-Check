{
	"name": "nextcloud/updatestatusinfluxdb",
	"description": "Monitors if there are updates available and sends at least a daily notification to an InfluxDB.\n\nIf an update is available, it specifies also the current version and the version it will update to.",
	"license": "AGPL-3.0-or-later",
	"authors": [
		{
			"name": "Philipp Kolmann",
			"email": "philipp@kolmann.at",
			"homepage": "https://github.com/pkolmann/Nextcloud-Update-Check"
		}
	],
	"autoload": {
		"psr-4": {
			"OCA\\UpdateStatusInfluxDB\\": "lib/"
		}
	},
	"scripts": {
		"post-install-cmd": [
			"@composer bin all install --ansi"
		],
		"post-update-cmd": [
			"@composer bin all update --ansi"
		],
		"lint": "find . -name \\*.php -not -path './vendor/*' -not -path './vendor-bin/*' -not -path './build/*' -print0 | xargs -0 -n1 php -l",
		"cs:check": "php-cs-fixer fix --dry-run --diff",
		"cs:fix": "php-cs-fixer fix",
		"psalm": "psalm --threads=1 --no-cache",
		"test:unit": "phpunit tests -c tests/phpunit.xml --colors=always --fail-on-warning --fail-on-risky",
		"openapi": "generate-spec"
	},
	"require": {
		"bamarni/composer-bin-plugin": "^1.8",
		"php": "^8.1"
	},
	"require-dev": {
		"nextcloud/ocp": "dev-stable29",
		"roave/security-advisories": "dev-latest"
	},
	"config": {
		"allow-plugins": {
			"bamarni/composer-bin-plugin": true
		},
		"optimize-autoloader": true,
		"sort-packages": true,
		"platform": {
			"php": "8.1"
		}
	}
}
